import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as TagStories from './Tag.stories';
import { DocsTabs } from '../../docs/components/DocsTabs';

<Meta of={TagStories} />

# Tag

Основано на `docs/templates/component-documentation-template.md`.

<DocsTabs
  tabs={[
    {
      id: 'overview',
      label: 'Обзор',
      children: (
        <>
          <h2>Обзор</h2>
          <p>
            Tag — компактный паттерн меток для коротких метаданных, быстрого выбора,
            удаляемых значений и вторичных действий. Публичная обертка Tag сохраняет
            совместимость со старым использованием, а явные компоненты семейства Tag
            точнее описывают ожидаемое поведение.
          </p>
          <h3>Когда использовать</h3>
          <ul>
            <li>Используйте ReadOnlyTag для статичной информации, которую нужно только прочитать.</li>
            <li>Используйте SelectableTag для выбора с поведением checkbox.</li>
            <li>Используйте DismissibleTag для значения, которое можно удалить.</li>
            <li>Используйте OperationalTag для компактного действия или ссылки.</li>
          </ul>
          <h3>Когда не использовать</h3>
          <ul>
            <li>Не используйте теги как основные действия страницы.</li>
            <li>Не используйте read-only тег, если элемент меняет состояние или ведет по ссылке.</li>
            <li>Не передавайте смысл только цветом.</li>
          </ul>
        </>
      ),
    },
    {
      id: 'design',
      label: 'Дизайн',
      children: (
        <>
          <h2>Дизайн</h2>
          <h3>Наборы компонентов</h3>
          <Canvas of={TagStories.ComponentSets} />
          <h3>Анатомия</h3>
          <p>
            Теги используют скругленный контейнер, текстовую обертку, необязательный контент
            с иконкой слева или справа и controls, зависящие от поведения, когда компонент
            интерактивный.
          </p>
          <h3>Спецификация</h3>
          <ul>
            <li>Поддерживаемые размеры: tiny, small, medium и large там, где они доступны в конкретном Tag-компоненте.</li>
            <li>Поверхности Tag используют семантические токены фона, текста, border, radius, spacing и typography.</li>
            <li>Длинные labels остаются в одну строку и обрезаются ellipsis внутри ширины компонента.</li>
            <li>Поведение светлой и темной темы приходит из слоя семантических токенов.</li>
          </ul>
        </>
      ),
    },
    {
      id: 'development',
      label: 'Разработка',
      children: (
        <>
          <h2>Разработка</h2>
          <h3>Импорты</h3>
          <pre><code>{`import {
  DismissibleTag,
  OperationalTag,
  ReadOnlyTag,
  SelectableTag,
  Tag,
} from 'ufo-design-system'`}</code></pre>
          <h3>Публичный API</h3>
          <p>
            Для нового кода предпочитайте явные компоненты семейства Tag. Legacy-обертка Tag
            поддерживает режимы: multi, single, operational, link, readonly и dismissible.
          </p>
          <h3>Использование</h3>
          <pre><code>{`<ReadOnlyTag label="Новинка" color="blue" />
<SelectableTag v-model="selected" label="Москва" value="moscow" />
<DismissibleTag label="Москва" close-aria-label="Удалить Москву" @dismiss="remove" />
<OperationalTag label="Открыть каталог" href="/catalog" />`}</code></pre>
          <p>
            Props, состояния и примеры, зависящие от поведения, смотрите в документации
            конкретных компонентов.
          </p>
        </>
      ),
    },
    {
      id: 'accessibility',
      label: 'Доступность',
      children: (
        <>
          <h2>Доступность</h2>
          <ul>
            <li>ReadOnlyTag рендерит статичный текст и не попадает в tab order.</li>
            <li>SelectableTag использует нативную checkbox-семантику через input control.</li>
            <li>DismissibleTag предоставляет нативную кнопку закрытия и требует доступный close label.</li>
            <li>OperationalTag рендерит button для действий и anchor, когда задан href.</li>
            <li>Интерактивные теги должны сохранять эквивалентное поведение для клавиатуры и указателя.</li>
            <li>Focus-visible styles должны оставаться видимыми и token-based.</li>
          </ul>
        </>
      ),
    },
  ]}
/>
